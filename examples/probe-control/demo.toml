# Declarative spec for the probe-control demo.
# See examples/_common/README.md for the harness contract.
script = "probe.d"
workload = "auto"
expect_records = 0
expect_agg_rows = 1
runtime_seconds = 16
description = "Probe-control demo — exercise the OBSERVER_ATTACH/DETACH protocol with idle workload."
