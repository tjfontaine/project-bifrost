# Declarative spec for the intra-guest-http demo.
# See examples/_common/README.md for the harness contract.
script = "probe.d"
workload = "auto"
expect_records = 1
expect_agg_rows = 0
runtime_seconds = 16
description = "Trace nginx + ab inside a smolvm guest via fbt; no port-forward (loopback in-VM)."
