# Declarative spec for the scheduler-offcpu demo.
# See examples/_common/README.md for the harness contract.
script = "probe.d"
workload = "auto"
expect_records = 0
expect_agg_rows = 1
runtime_seconds = 16
description = "Trace stress-ng off-CPU latency via fbt:sched_switch; quantize the wakeup distribution."
