# Declarative spec for the cross-domain-http demo.
# See examples/_common/README.md for the harness contract.
script = "probe.d"
workload = "auto"
expect_records = 0
expect_agg_rows = 1
runtime_seconds = 16
description = "Cross-domain trace: macOS dtrace + smolvm guest fbt against an nginx + ab workload."
